plugin: aws_ec2
regions:
  - us-east-2
filters:
  instance-state-name: running
  tag:Name: "{{ lookup('env', 'INSTANCE_NAME') }}"
hostnames:
  - instance-id
keyed_groups:
  - key: tags['Name']
    prefix: ""
